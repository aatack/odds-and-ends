{
  "name": "looper",
  "version": "0.1.0",
  "description": "Keeps a Claude agent looping on one long-running task in the background, pinging you on Telegram when it needs you.",
  "type": "module",
  "bin": {
    "looper": "bin/looper"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
